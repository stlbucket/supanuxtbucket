query IncidentById(
  $incidentId: UUID!
) {
  incident(id: $incidentId) {
    id
    name
    description
    status
    identifier
    todo {
      id
      name
      description
    }
		topic {
      id
      name
    }    
  }
}