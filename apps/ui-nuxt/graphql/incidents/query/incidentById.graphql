query IncidentById(
  $incidentId: UUID!
) {
  incident(id: $incidentId) {
    id
    name
    description
    status
    identifier
    isTemplate
    todo {
      id
      name
      description
    }
		topic {
      id
      name
    }    
  }
}