query SearchIncidents(
  $searchTerm: String
) {
   searchIncidents (
    _options: {
      searchTerm: $searchTerm
  	}
  ){
    nodes {
      id
      name
      description
      status
      identifier
    }
  }
}