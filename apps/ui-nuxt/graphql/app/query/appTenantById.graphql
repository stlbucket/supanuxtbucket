query TenantById(
  $tenantId: UUID!
) {
  tenantById(id: $tenantId) {
    id
    name
    identifier
    status
    type
    createdAt
    profileResidents: profileResidentsByTenantId {
      totalCount
    }
    tenantSubscriptions: tenantSubscriptionsByTenantIdList {
      id
      licensePackKey
      licenses:licensesByTenantSubscriptionId {
        totalCount
      }
    }
  }
}